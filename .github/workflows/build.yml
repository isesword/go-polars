# name: CI Build

# on:
#   push:
#     branches: [esword]
#   pull_request:

# jobs:
#   build:
#     runs-on: ${{ matrix.os }}
#     strategy:
#       fail-fast: false
#       matrix:
#         os: [ubuntu-latest, macos-latest, windows-latest]
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Rust
#         uses: dtolnay/rust-toolchain@stable

#       - name: Setup Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.25.x"

#       - name: Install make (Windows)
#         if: runner.os == 'Windows'
#         shell: pwsh
#         run: choco install make

#       - name: Build
#         shell: bash
#         run: ./build.sh --force

#       - name: Upload artifact (Linux/macOS)
#         if: runner.os != 'Windows'
#         uses: actions/upload-artifact@v4
#         with:
#           name: polars-go-${{ runner.os }}
#           path: polars/bin/libpolars_go.a

#       - name: Upload artifact (Windows)
#         if: runner.os == 'Windows'
#         uses: actions/upload-artifact@v4
#         with:
#           name: polars-go-${{ runner.os }}
#           path: polars/bin/polars_go.lib
